#!/bin/sh
set -eu

echo "Running business reference contracts before push..."

exec /bin/sh "${0%/*}/run-heavy.sh" npm run test:business-references -- --maxWorkers=1 --no-file-parallelism --exclude '.claude/worktrees/**'
