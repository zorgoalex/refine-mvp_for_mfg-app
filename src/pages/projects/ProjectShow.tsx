import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

export const ProjectShow: React.FC = () => (
  <div style={{ padding: 24 }}>
    <Title level={3}>Проект</Title>
  </div>
);
